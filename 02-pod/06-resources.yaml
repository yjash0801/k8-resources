apiVersion: v1
kind: Pod
metadata:
  name: resources-pod
  namespace: mechanoidstore
spec:
  containers:
  - name: resources-pod
    image: nginx
    ports:
    - containerPort: 80
    resources:
      requests:
        cpu: "100m"
        memory: "68Mi"
      limits:
        cpu: "200m"
        memory: "128Mi"